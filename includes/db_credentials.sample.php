<?php
// Copy this file to includes/db_credentials.php ON THE LIVE SERVER ONLY
// (via cPanel File Manager, not git) and put the real database password
// below. That file is gitignored, so it stays untouched by future pulls.
define('DB_PASS', 'your-real-cpanel-database-password');

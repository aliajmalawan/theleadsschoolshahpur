# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# Logs
*.log

# Local environment overrides (if ever added)
.env

# Real live DB password — lives only on the production server, never in git
includes/db_credentials.php
